/**
 * POST /setup/instance-admin
 * Creates a new instance admin.
 */

import { Router } from "express";

const router = Router();

router.post("/", async (request, response) => {

  // Create admin role.

  // Create admin user.

  // Add this event to the activity log.

  // Return the new admin user.

});

export default router;